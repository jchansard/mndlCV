import { subtractHelper } from 'personal-site/helpers/subtract-helper';
import { module, test } from 'qunit';

module('Unit | Helper | subtract helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subtractHelper([42]);
  assert.ok(result);
});
