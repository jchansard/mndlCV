import { boldFirstPiece } from 'personal-site/helpers/bold-first-piece';
import { module, test } from 'qunit';

module('Unit | Helper | bold first piece');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = boldFirstPiece([42]);
  assert.ok(result);
});
